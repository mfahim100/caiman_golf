import 'package:flutter/material.dart';

class InviteTab extends StatelessWidget {
  const InviteTab({super.key});

  @override
  Widget build(BuildContext context) {
    return  const Center(
      child: Text('Invite Tab'),
    );
  }
}
